# OpenRPG (skeleton)

This repo contains a minimal Java multi-module Gradle project with a `server` module and basic user store tests (H2 + bcrypt). This is a starter to be expanded into the full project.